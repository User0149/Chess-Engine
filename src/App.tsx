import GameContextProvider from "./context/GameContext";

function App() {
    return (
        <GameContextProvider>
            UI in progress.
        </GameContextProvider>
    )
}

export default App;